import type { Metadata } from "next";
import CartContent from "./cart-content";
export const metadata: Metadata = { title: "Keranjang | Alinara" };
export default function CartPage() { return <CartContent />; }
